{% liquid
  if id == blank
    return null
  endif

  graphql r  = '<%= options.outputLogicFolder %>/<%= modelNamePlural %>/search', id: id, limit: 1

  return r.<%= modelNamePlural %>.results.first
%}
