{% liquid
  if id == blank
    return null
  endif

  graphql r  = '<%= config.outputLogicFolder %>/<%= modelNamePlural %>/search', id: id, limit: 1

  return r.<%= modelNamePlural %>.results.first
%}
