{% liquid
  assign c = '{ "valid": true, "errors": {} }' | parse_json

  function c = '<%= options.logicDependencyFolder %>/commands/validations/presence', c: c, object: object, field_name: 'id'

  hash_assign object['valid'] = c.valid
  hash_assign object['errors'] = c.errors

  return object
%}
