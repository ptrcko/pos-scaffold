{% liquid

  include '<%= config.logicDependencyFolder %>/login_or_redirect'

  content_for page_title
  print 'app.models.<%= modelNamePlural %>.new' | t
  endcontent_for
  include '<%= config.outputThemeFolder %>/simple/<%= modelNamePlural %>/new'
%}
